export enum ErrorStatusCode {
  P2Error = 'P2002',
}

export enum gender {
  male,
  female,
  others,
}

export enum EmployeeDesignation {
  intern,
  employee,
  hr,
}

export enum leave {
  open,
  approved,
  reject,
  pending
}